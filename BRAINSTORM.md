# AQ2 Texture Swapper — Brainstorm & Plan

*Started 2026-08-31. This is the living planning doc for the project.*

## The idea

A program for AQ2/AQtion players that lets you restyle any map's textures for gameplay purposes:

- See all textures used on a map, ranked by how much they're used
- Swap any of them for another texture and preview the result
- Swap the map's skybox too
- Save your setup as a per-map preset, reset to defaults anytime
- Share presets with friends as small files
- Later: also save lighting settings (gl_modulate etc.) into presets
- Long term: grow into an **AQ2 hub** — nick/player setup, server browser, connect & launch the game from the app

## Facts learned from the install (verified 2026-08-31)

Install inspected: `C:\AQ2mapping\AQ2` — this is **AQtion** (q2pro-based engine: `aqtion.exe`, `q2pro.exe`), game data in `baseaq\`.

- `baseaq\pak0.pkz` (344 MB, a renamed zip): main content. 3386 texture files (2404 `.wal`, 636 `.png`, 515 `.tga`, 103 `.jpg`), 18 `.bsp` maps, plus `env/` skyboxes.
- `baseaq\2023_25thAnniversary_mapjam.pkz` (102 MB): extra maps. The app must scan **all** pkz/pak files plus loose files.
- `baseaq\pak1.pkz`: default configs — **contains the softlink system**:
  - `softlink <virtual-path> <target-path>` is a console command that remaps one file path to another at runtime, e.g. `softlink textures/e1u2/box1_3.wal textures/e1u1/box1_3.wal`
  - AQtion ships `tex-e1u2.cfg` … `tex-e3u3.cfg` full of these, chained from `textures-action.cfg` via `exec`
  - Links are applied per file (`.wal`, `.pcx`, `.tga` linked separately); community docs also show directory-level links (`softlink textures textures-hd`)
  - Softlinks are runtime state, applied by exec'ing cfg files → **per-map presets = exec a different cfg per map**
- `baseaq\README.md` says: never modify shipped pkz files; add your own files alongside. The app will follow this — everything it writes is additive and reversible.

## Decisions (interview 2026-08-31)

| Topic | Decision |
|---|---|
| Installs | User has **several installs** → app has folder picker + install profiles |
| Preview | **Game path first**: V1 applies swaps to the real game with instant reload; V2 adds a built-in 3D map viewer |
| Swap sources | **Stock AQ2 textures** + **generated flat/clean textures** (visibility mode). Custom image import = maybe later |
| Distribution | **Single portable .exe** (no installer) |
| Applying presets | **Automatic on map load** (game hook), keybind as fallback |
| Sharing | **Export/import preset files** (Discord-friendly) |
| Skyboxes | **Yes, in V1** — swap a map's skybox like any texture |
| Sounds | Not now; maybe later (same softlink mechanism works for `.wav`) |
| Long-term vision | Grow into an **AQ2 hub**: nick/player setup, server browser, connect from the app |

## How V1 works (architecture)

Stack: **Electron** (Node backend + web UI) → portable .exe (~90 MB), and the web UI later becomes the 3D viewer (three.js) in V2.
*(Originally planned as Tauri for a ~10 MB exe, but this machine has Node 24 and no Rust/MSVC toolchain — Electron avoids a multi-GB compiler install and all core logic is plain JS either way. Decided 2026-08-31.)*

1. **Read game data** (read-only): open all pkz/pak in the chosen install's `baseaq`.
   - Parse `.bsp`: texinfo lump lists texture names; faces lump gives usage counts → "most used" ranking
   - Decode `.wal` (8-bit + Q2 palette from `pics/colormap.pcx`), `.pcx`, `.tga`, `.png`, `.jpg` → thumbnails
2. **UI**: pick install profile → pick map → texture grid sorted by usage → click texture → choose replacement (browse/search stock textures, or generate flat/clean texture with color + brightness choice) → before/after thumbnails.
   - **Skybox panel**: the map's current sky is read from the BSP's worldspawn `sky` key; show all available `env/` skyboxes as previews and swap (via `sky <name>` command in the map cfg if supported, else 12 `env/` softlinks like AQtion's own configs do).
3. **Apply**: write `baseaq/texswap/<mapname>.cfg` with softlink lines. One-time setup adds a hook (e.g. `cl_beginmapcmd "exec texswap/$mapname.cfg"`) so the game auto-applies on every map load. Keybind (e.g. F9: exec + texture flush) for applying while in a map.
4. **Reset**: per-map or global — remove/empty our cfg files, plus in-session unlink or reload. Shipped game files are never touched.
5. **Presets**: stored as JSON per map per profile; export/import as a single small file.

## Progress

- **2026-08-31 — Milestone 1 done**: scanner + browser working against the real install. Reads loose files + `.pak` + `.pkz`; parses maps with titles, skyboxes, per-texture face counts and visible-area ranking; decodes WAL (palette), PCX, TGA, PNG, JPG thumbnails; dark UI with map list, search, sort, utility-surface toggle, skybox card. Run with `npm run dev`.
- **2026-08-31 — Milestone 2 done (engine-verified)**: full swap pipeline working.
  - Scanner now mirrors the engine's real search path: `action/` layered over `baseaq/`, non-numbered archives > numbered pakN > loose files, plus the shipped soft-link fallbacks — 1804 maps and 20,884 textures found on the mapping install.
  - Swap UI: click texture → picker (stock textures with search, or generated flat/grid visibility textures), skybox picker, per-swap remove, per-map reset, per-map swap badges.
  - Writes `<modDir>/texswap/<map>.cfg` for every map (hard `link` lines + `unlink --all` + `r_reload`), generated textures in `texswap/gen/` (same-extension transcodes: PNG/JPG/TGA/WAL encoders incl. palette quantization + mipmaps), `presets.json`, `hook.cfg`, and one appended line in `autoexec.cfg`.
  - Proven headlessly with q2proded (exec chain autoexec → hook → map cfg; texture and sky links resolve to our generated files) and then **confirmed live in the user's q2pro client on urbanjungle**: flat-grid walls, stone ground, swapped sky, lightmaps intact. One fix was needed along the way: `${cl_mapname}` must be braced in the hook (plain `$cl_mapname.cfg` parses the dot into the macro name and expands empty).

- **2026-08-31 — Milestone 3 done**: named presets per map (save/load/delete chips), export to `texswap/exports/<map>.aq2swap.json` for Discord sharing, import with validation (missing replacement textures skipped with warnings, unknown maps stored for later), and a master "Swaps: ON/OFF" toggle that parks every map at stock while keeping all presets. Verified end-to-end incl. export→import roundtrip.

- **2026-08-31 — Feedback round 1** (user request): fixed grid pattern (lines now centered per tile → true symmetric grid, previews and tiling both clean); added checker/stripes/diagonal patterns with visual style buttons; Flat tab now also offers the user's **ralle_colors** Quake-palette WALs (246 flats in `action/textures/ralle_colors`) as a clickable palette; folder **Browse…** dialog with "looks like an AQ2 install" detection plus a **?** help popup for the path; active preset chip is highlighted.

- **2026-08-31 — Lighting milestone**: opt-in managed lighting — global defaults + per-map overrides for `gl_modulate`, `gl_modulate_world/entities`, `gl_brightness`, `intensity`, `gl_saturation`, `gl_coloredlightmaps`, `gl_dynamic`, `gl_picmip`, `r_override_textures` + free-form extra cfg lines. (`r_texture_overrides` mask parked at user request until documentation is found — settable via extra lines meanwhile. `gl_brightness` default is ~0.1 per user.) Written into every map cfg when managed so nothing leaks between maps; included in saved presets and export/import. Also fixed user-reported bug: clearing a swap + F9 didn't revert visually (touched maps now always `r_reload`, and the F9 bind appends `r_reload` too).
- **Confirmed final scope before packaging** (user, 2026-08-31): 3D viewer, custom image import, sound swaps, team pack sharing, polish — THEN the portable .exe.

- **2026-08-31 — Custom images + invisible swaps** (user request, generalized to all textures): "Your image" tab in the picker uploads png/jpg/tga (stored as master PNG in `texswap/custom/`, transcoded per format like any swap; embedded base64 in export files so sharing works); "Make invisible" swaps to a fully transparent png/tga — hi-res-path only (WAL has no alpha; low-res mode keeps the original gracefully), flagged experimental pending in-game confirmation on opaque surfaces. Recents/favorites/low-res-preview shipped same day.

- **2026-08-31 — Favorite collections** (user request, aimed at mapmakers): named texture sets ("great bricks", "Makkons best metal", …) on top of the â˜… All-favorites list. Any texture joins any number of collections via a ï¼‹ popup in the picker; a dropdown filters the picker to a collection; adding to a collection auto-stars (All favorites stays the superset); collections deletable. Future synergy: include collections in team-pack sharing.

- **2026-08-31 — UI design-system overhaul** (user request, done overnight): stylesheet rewritten as one coherent system — token set (4 layered surfaces, one orange accent + semantic good/bad/info, 6/10/14 radii, 2 shadows), exactly two typefaces (Segoe UI Variable + Cascadia Mono/Consolas), uppercase micro-labels, unified 32px controls & button variants (base/primary/danger/small), brand stripe + gradient logo, refined cards (hover lift, swap rings), chip rail for presets, underline tabs, consistent modal chrome with animations, styled scrollbars, toast slide-ins, glass viewer bars. All class names/IDs preserved — zero behavior changes.

- **2026-08-31 — Durable storage**: presets/collections/custom-image masters moved to `%APPDATA%\AQ2TextureSwapper\installs\<slug-hash>\` (per install), with silent migration from the old in-install location. Game-side files (per-map cfgs, gen/, exports) stay in the install and are fully regenerable — after a game reinstall, one click on Install game hook rebuilds everything. First-run UX: prominent setup banner with one-click hook install; in-app ðŸ“„ Readme dialog replaced the ?-help; hook chip removed (button only when missing).

- **2026-09-04 — Skin studio (weapon models) shipped in v1.5**: new full-screen studio (`🔫 Weapon skins` in the Swaps panel) for the md2 weapon models. Server side: `core/md2.js` (IDP2 parser, per-frame vertices, UV wireframe), `core/pcx.js` (8-bit RLE encoder, Q2 palette), `core/tools.js` (fetches Real-ESRGAN ncnn/Vulkan from its official GitHub release into `%APPDATA%\AQ2TextureSwapper	ools`, 45 MB, on demand), `core/skins.js` (SkinStore: masters in app-data `skins/` + `skins.json`, materialized to `<modDir>/texswap/skins/<weapon>/`). UI (`ui/skins.js`): weapon list with live thumbs, three.js preview of the real md2 with all frames animating at 10 Hz (game view from the player's eye or orbit), 2D skin view with UV overlay / stock compare, upload skin (png/jpg/tga/pcx), replace model (.md2, validated), AI upscale 2x/4x (detailed or smooth model, ~4 s on an RTX 4060), color adjust (hue/sat/brightness/contrast previewed live on the model, baked on apply), one-step undo, UV template export, `.aq2skin.json` export/import (skin + model embedded; Import preset… recognizes it), Skins ON/OFF. First real use: the AK-47 model + upscaled skin replacing the M4 view model - verified in game.
  - **Apply mechanism (engine-verified 2026-09-04)**: `link models/weapons/v_m4/tris.md2 texswap/skins/v_m4/tris.md2` (+ `skin.png`/`skin.pcx`, and `.tga`/`.jpg` when the install has one) works for models and skins exactly like textures; `whereis` confirms. The lines are written INLINE into hook.cfg (startup, so the first map already loads them) and into every map cfg right after `unlink --all`. Skins are global (not per map), independent of the Swaps ON/OFF toggle.
  - **Gotcha: never add an `exec` to the startup chain.** q2pro's `Cmd_Exec_f` counts nested execs against `ALIAS_LOOP_COUNT` per command-buffer run; a normal AQtion startup (default.cfg → textures-action.cfg → 7 tex-*.cfg → q2config → autoexec → hybridjump → hook.cfg …) already sits at the edge, and one more `exec texswap/skins.cfg` made AQtion's own `postinit.cfg` fail with "Infinite loop avoided". Hence inline lines.
  - Skin aspect check compares uploads against the install's actual skin image (AQ2 ships 1280x400 pngs for 512x256 md2 layouts - the header aspect is NOT the reference), or against the replacement model's header when a model is swapped.
  - **Engine limit (found live 2026-09-04): images above 4096px on a side fail with "Couldn't load …skin.png: invalid image dimensions" (q2pro MAX_TEXTURE_SIZE).** Masters may be bigger; the game copy is fitted to 4096 at materialize, and AI upscale picks the largest of 4x/3x/2x that fits (a 1280x400 stock skin gets 3x).
  - Model changes need a map restart in game; skin changes show after F9 (`r_reload`) or a map restart. Only view (`v_*`) and world (`g_*`) weapon models under `models/weapons/` are listed; player-held `w_*` vwep models are not covered yet.
  - Next ideas: AI *generation* of skin variants (img2img with the UV layout as guide - needs a local Stable Diffusion or an online image API, undecided), vwep/player models, rigid-part animation transfer so any imported gun mesh inherits a stock weapon's hand animation (see 2026-09-04 discussion).

- **2026-09-04 (later) — Restyle + Collection**: user decision: no AI generation inside the app (players can't be expected to have API keys; a local Stable Diffusion would mean a 2 GB download and a strong GPU). Instead: (1) **Restyle mode** in the studio (`ui/restyle.js`, pure canvas, works on every PC): 18 base materials as gradient maps over the skin's own shading (gold, rose gold, chrome, brushed steel, gunmetal, black ops, copper, bronze, olive, desert tan, arctic, walnut, cherry, red, navy, toxic green, purple, hot pink) with a strength slider; pattern overlays (woodland/desert/urban/arctic/digital camo, carbon fibre, hex plating, racing stripes, scratches & wear, grit) with size + opacity, lit by the pixel's luminance so shading survives; hue/sat/brightness/contrast; **protect boxes** dragged on the skin image keep areas (hands) untouched. Previewed live at 1024 px on the model, baked at the master's full size on apply. (2) **Collection** (`core/library.js`): `.aq2skin.json` files from `<app>/library/**` (ships with the build, read-only, `package.json` files includes `library/**`) and the install's app-data `library/` (saved via ★ Save to collection…, imported from friends - Import preset… now stores AND applies skin files). Cards per weapon with thumbnails; click applies; own entries deletable. (3) Ralle will make 3-4 stock variants per weapon with his own AI and drop them into `library/<weapon>/`; UV templates + stock skins for all 9 view weapons were written to `C:\AI\AIprojectsq2models	emplates\` for that. Note: the user was live-testing the studio during the build (handcannon, SSG and MP5 got AI-upscaled skins on the play install) - those are his.

- **2026-09-04 (later still) — AI upscale of map textures** (user idea: crisp versions of classic low-res maps like urban/teamjungle, same art, nostalgia intact): new swap type **`upscale`** `{type:'upscale', factor:2|3|4}` and `core/upscale.js` (TextureUpscaler). Per-map dialog **✨ AI upscale…** in the map controls: plan (eligible / already hi-res above a threshold 512·1024·2048 / other swaps / missing / over 4096), background job with progress + cancel, results stored as swaps in one bulk save (`setSwapsBulk`), **Remove upscales**. Upscales replace ONLY the hi-res override exts (.png + existing .tga/.jpg), never the .wal → tiling untouched, low-res mode (15) stays stock, visible in override mode (31). Factor drops to the largest that keeps ≤ 4096 px. Cache: `%APPDATA%/AQ2TextureSwapper/upscale/<sha1(source)>-x<f>.png` keyed by texture CONTENT → shared by every map/install; alpha (index-255 holes, tga alpha) is re-applied nearest-neighbour after the RGB upscale. First run on 1am: 74 textures in 3m45s on the RTX 4060, 187 MB cache. Presets/exports carry only the spec; a receiver without the files gets one summary warning and runs the job themselves (materialize skips the link until the cache exists). Thumbs: `/api/thumb?upscale=<tex>&factor=`. Viewer tiles 1:1 (same UVs). **Per-texture choice (user decision: not every texture suits upscaling - signs, decals):** the dialog lists every eligible texture as a thumbnail checkbox (all ticked by default, All/None buttons; the start button counts the selection) and the job takes `names`. The texture picker (click a card) has **✨ AI upscale this texture** for single textures (uses the dialog's remembered scale/source; an existing upscale is redone). **Keep grain (user: grainy walls come out smudged):** `addGrain()` in core/upscale.js - bilinear-enlarge the source to the AI size, subtract a wrap-around box blur (radius ~0.75×factor, two passes) to get its high-pass, add k× that over the AI result (k = 25/50/75/100 %). Per-texture `spec.grain`, cache suffix `-g<n>`, fifth dropdown 'Keep grain' in the full-screen view, single-texture button honours it. Evidence: e2u3/blume6_2 at 0/50/100 - 0 = paint, 100 = full speckle back, structure from the AI either way. Pure-noise textures still cannot gain structure. **Full-screen AI upscale view (user request 2026-09-04: modals → full-screen like the Skin studio; texture picker is next):** `#upscaleOverlay` in index.html, `UP` state + openUpscaleMap/upRefresh/upRenderSide/upRenderGrid in main.js. Left panel: four `<select>` dropdowns (source, look, scale, skip size - replacing the button rows), selection summary with All/None, run button with progress bar + cancel, remove upscales, cache line + clear unused. Right: card grid (256px thumbs, tick mark, badge with size→factor or 'done · 4x smooth · wal'), filter box. Esc closes; the map view reloads on close if anything changed. A finished run leaves nothing ticked; done textures are opt-in. **Cache housekeeping:** the dialog shows the cache size (`/api/upscale/cache`), files no map/preset references count as unused and **Clear unused** deletes them (`/api/upscale/clearcache`; regenerated on demand); past 1 GB the line turns red and the app toasts once per session after a scan (`scanResult.upscaleCache`). Also: an explicit regen note when a map's preset lacks generated files on this PC (import / cleared cache). **Redo in place (user hit 'nothing to do' after a run):** already-upscaled textures stay in the dialog's grid, unticked, labelled 'done · 4x smooth · wal'; ticking one redoes it with the CURRENT settings (start() takes such names and rebuilds with factor/src/model of the dialog). 'All' ticks only not-yet-done ones. **Look per run (detailed / smooth):** NOTE: animevideov3's x2 weights in this ncnn build return half-transparent garbage (user saw green noise on the lamp in game), so BOTH models always run at 4x and get box-filtered down; smooth cache suffix is `-s2`. the map dialog and the single-texture button take `model` (`realesrgan-x4plus` vs `realesr-animevideov3`); stored in the spec (`model:'smooth'`), cache key suffix `-s`, regen keeps it. Evidence strip (lamp/door/rock/bricks): smooth keeps fine geometric patterns (checker lamps, grates) that detailed flattens; detailed keeps grime and stone edges that smooth flattens. Workflow: walls detailed, then tick just the lamps/grates and rerun smooth. **2x/3x bug (user spotted on aboveclouds: lamp and door 'did not fit'):** realesrgan-x4plus is a 4x-only network; the ncnn tool asked for `-s 2` returns a 2x-sized CROP of the 4x result (a zoomed corner), so `upscalePng` now always runs 4x for the detail model and box-filters down (animevideov3 has real x2/x3 weights). Cache recipe v3 for factors 2/3. Also engine-verified (images.c get_image_dimensions): a hi-res override keeps its tiling grid only if the ORIGINAL .wal exists; tga/png-only textures would tile by the upscaled image size, so `plan()` excludes them (`noGrid`), the swap store purges such stored swaps, import refuses them. Tiny high-frequency patterns (1-px checker lamps) get smoothed flat by the network - that is inherent; the per-texture checkboxes are the answer. **Two fixes found live on teamjungle (user spotted both):** (a) upscales are made from a WRAP-PADDED source (16 px wrapped border, cropped after) so tiling edges stay seamless; cache keys carry a recipe tag (`-v2`) and `plan()` regenerates upscale swaps whose cache file is missing (recipe change, cleared cache, imported preset). (b) gen file names carry the cache file's content hash (`up-<tex>-<hash>-x4-v2.png`) - the first version named them `up-<tex>-x4` only, so re-running with the .wal source silently kept the hi-res-based file (engine `imagelist` showed 2048 grass instead of 256). `imagelist` in the console is the definitive check of what the engine loaded. Test gotcha: in scripted `+exec` test runs the `cl_beginmapcmd` exec lands BEHIND the script's remaining commands in the buffer, so a `quit` in the script runs first and the map cfg never executes - chain the probe via cl_beginmapcmd instead.

- **2026-09-04 (evening) — Model overhaul, milestone 1: MD3 pipeline proven.** Ralle's own project (better hands / weapon / player models for the community; AQ2+AQtion assets are free to modify and share). Facts: his AQtion q2pro.exe (r1~c981fcb, Oct 2023) loads MD2 + MD3 by header ident (so `link tris.md2 → file.md3` works), NO MD5; per-mesh limits in that build are 4096 tris / 4096 verts ("Too many elements"), newer source says 6144; a whole model must stay under ~4 MB of hunk. Blender 4.5.13 LTS portable in `C:\AI	oolslender-4.5.13-windows-x64\` (headless only). `tools/md2_to_md3.py`: MD2 → Blender mesh with UVs → optional Catmull-Clark subdivision (`--subdiv N --smooth`) restricted to skin-UV regions (`--region u0,v0,u1,v1`, e.g. the hands) → MD3 with per-vertex normals, surfaces auto-split at 4000 tris, frame names/timing preserved 1:1. Studio: `core/md3.js` parser + `md3AsMd2` adapter, upload accepts .md3, materialize writes `tris.md3` and links tris.md2 to it, preview works. Verified in game: plain M4 MD3 identical to stock; hands-subdivided M4 (4667 tris, 2 surfaces) loads and animates. Saved in the collection as "M4 MD3 demo (smooth hands)"; Ralle's AK restored. ENGINE UPDATE (same evening, Ralle's go): play install now runs the Steam AQtion engine r1~ddb58b8 (May 2026) with the matching Steam game dll (the Feb-2026 game dll crashed with the new engine; backups in `_backup_engine_2023/`). It has gl_md5_load/use = 1 → MD5 skeletal replacement models are possible; MD3 per-mesh limit 6144; `gl_shaders 0` in Ralle's cfg keeps the legacy backend (4 MB model hunk), `gl_shaders 1` = modern backend with 16 MB. NEXT: real new hand geometry (CC0 asset or AI mesh) fitted to the existing hand animation frame by frame; later a player-model demo (198 frames, same technique).

## Verified engine facts (tested against AQtion q2proded + source, 2026-08-31)

- **`softlink` is fallback-only** — it fires only when the requested file does not exist. **`link` (hard) expands before the file search and overrides existing files → the app uses `link`.** Same syntax; `unlink --all` clears only hard links (AQtion's shipped soft links live in a separate list — clean namespace separation).
- Links are prefix-matched (directory links work, used for skybox swaps: `link env/<from> env/<to>`), cleared on `fs_restart`, applied per exec.
- Cross-extension links resolve at FS level, but the image decoder is chosen by the *requested* extension → the app transcodes replacements to matching extensions.
- `cl_beginmapcmd` + `$cl_mapname` macro exist (client), `r_reload` refreshes textures in-game, `whereis` resolves links (great for debugging).
- **Hi/low-res texture settings** (from user): `r_override_textures` enables truecolor override of WAL/PCX; `r_texture_overrides` is a bitmask choosing which categories (world textures, skins, HUD, console) use hi-res — user runs 15 (world low-res) and toggles 31 (world hi-res). The app links **all** relevant extensions (existing + canonical .png and .wal) so swaps work in both modes. A per-preset hi/low toggle is planned for V3.
- **Texture alpha on world surfaces** (in-game test by user, 2026-08-31): a transparent replacement makes the surface invisible **only on trans-flagged surfaces** (SURF_TRANS33/66 — signs, glass, overlays); on opaque surfaces the engine renders the transparent areas **solid black**.
- **Invisible feature REMOVED before v1.0 release** (user decision, 2026-08-31): see-through fences/grates are a cheat in a competitive shooter, so no obvious one-click button. Removed the UI option, the server rejects `type: invisible` (set + import), and stored invisible swaps/presets are purged on load. Deliberately NO transparency policing of custom uploads (user decision): determined cheaters can drop files in the texture folder by hand anyway, and legit alpha cutouts must stay easy.

## Research: multi-game support (2026-09-01, no implementation yet)

Sound swaps dropped by user decision (sounds are map-specific). Instead: can the app cover more games? Verdict: **yes, in tiers** — the deciding factor per game is the *apply mechanism*, everything else (VFS, thumbnails, viewer, presets, packs) is reusable architecture.

The app's five per-game contracts: (1) file layout + archive formats, (2) BSP parsing, (3) texture decoding + palette, (4) how swaps reach the engine, (5) sky/lighting model.

- **Tier 1 — Quake 2 on q2pro-family (any mod: baseq2, ctf, rogue, xatrix, …): nearly free.** Same BSP/WAL/palette/env/pkz. `detectInstall` already recognizes `baseq2` and falls back to treating any content folder as a gamedir. q2pro `link` + `cl_beginmapcmd` works identically → full per-map feature parity. Effort: game-profile UX + branding + testing.
- **Tier 1.5 — Quake 2 on Yamagi / KEX rerelease (2023): global-only mode.** No `link` command in those engines, but all of them load loose files over paks (verified for the rerelease: extracted files under `rerelease\baseq2` take priority). Apply strategy = write actual texture files → a swap applies on *every* map using that texture. Honest, clearly-labeled degraded mode; still additive/reversible (app tracks its own files).
- **Tier 2 — Quake 1 (QuakeSpasm / Ironwail / vkQuake): possible, the real project.** Different BSP (v29, textures *embedded* as miptex), Quake palette, pak-only. Engines load external replacements from `textures/` (TGA/PCX in the QS family — our TGA encoder exists), and map-scoped variants (`textures/<mapname>/…`) are documented in the QS lineage → potentially per-map via plain files, no console mechanism needed (verify per engine before building). Viewer needs greyscale lightmaps + different sky handling. Weeks-scale.
- **Tier 3 — park it:** Quake 3/idTech3 (shader indirection, global-only), GoldSrc/Half-Life (WAD3 mess), Kingpin/SiN (custom engines, niche).

Architecture sketch: startup stays instant (splash + async scan already built) → **game profile picker** (profiles = {name, path, game, engine flavor} in APPDATA, remember last) → scan on selection. A **GameAdapter** layer per game: detect / vfs order / bsp parser / decoders / apply strategy (per-map cfg-links vs global loose-files) / sky / lighting cvars / launch command.

**Decisions (user, 2026-09-01):**
1. Order: **Quake 2 first** (broader community than AQ2), then **Quake 1**; Kingpin and Quake 3 as maybes.
2. **Per-map swapping is required** — global-only mode is rejected ("would destroy other maps"). Consequence: Q2 support targets **q2pro-family engines only** (Yamagi/KEX rerelease are out until they grow a link-equivalent); Quake 1 hinges on verifying per-map external texture dirs (`textures/<mapname>/…`) in QuakeSpasm/Ironwail — must be tested against the real engines before building; Kingpin and Q3 likely fail this bar (no per-map mechanism known) and stay parked unless one is found.
3. App gets a new name when it goes multi-game (small task, later; new logo via AI art).
4. UX: keep today's flow (remember last install), add an **in-game switcher** (dropdown/picker) since switching games is rare; startup splash later shows the supported games (AI art).
5. **Scope decision (2026-09-01): wrap up as a Quake 2 family swapper** (AQ2/AQtion + plain Q2 + any Q2 mod, all on q2pro engines). Quake 1 is postponed until the user has an install to test against — it stays the first planned expansion. Rename/rebrand also waits for that expansion. Q2 tier-1 shipped in v1.1.

- **V1 (core)**: everything above, including skybox swapping. Test in-game on user's machine, then beta with one friend.
- **V2 (wow)**: ✔ **shipped 2026-08-31 (v1)** — built-in 3D map viewer: server extracts triangulated per-texture geometry with real texinfo UVs (`/api/mapgeo`, wal-based texel scaling), three.js renders it with the current swaps applied (flat/stock/custom/invisible all honored — invisible shows trans surfaces gone and opaque ones black, mirroring the engine); fly controls (drag-look + WASD/QE/Shift), camera starts at a player spawn, hover shows the texture name, click a surface → "Swap this texture…" opens the picker, and every swap updates the viewer instantly. **Lightmaps added same day**: server unpacks the BSP lighting lump per face (16-texel luxel grid, style 0), shelf-packs a per-map RGB atlas (`/api/maplight` PNG) with 1px padding, emits `uv1` lightmap coords; viewer uses MeshBasicMaterial mapÃ—lightMap (texture.channel=1 — gotcha: three defaults to channel 0), intensity follows managed `gl_modulate`. Warp/unlit faces sample a white block. Remaining polish ideas: skybox rendering, warp-water animation.
- **V3 (presets+)**: ~~named presets + export/import files~~ ✔ done. Remaining: lighting presets (`gl_modulate`, `gl_modulate_world`, `gl_brightness`, `intensity`, …) saved alongside texture presets; hi/low-res toggle (`r_texture_overrides` 15/31) per preset; custom image import; sound swaps; "team pack" bundle sharing.
- **V1.1 startup polish ✔ done 2026-08-31**: the Electron window now opens instantly with a branded splash (ui/splash.html, loaded via loadFile before the server import), and the first map scan runs async server-side (Install.scanMapsAsync yields every 20 maps) while /api/scan reports `{scanning, progress}` and the UI polls it, showing "Scanning maps… 460 / 1804" live. Only the portable exe's own self-extraction (~2-5s before any window) remains, inherent to the portable format.
- **Packaging: ✔ v1.0 SHIPPED 2026-08-31** — Electron shell (`electron-main.mjs` runs the server in-process, window at 127.0.0.1:5892) + electron-builder portable target → `dist\AQ2TexSwap.exe` (~98 MB, one file, logo icon). First-run: friendly welcome/Browse prompt when no install found, orange setup banner for the hook. Verified end-to-end (boot, embedded server, 1804-map scan). Deferred to v1.1: ~~viewer skybox~~ ✔ done 2026-08-31 (cube backdrop from env faces via /api/skyface, honors sky swaps; Q2 faces mirrored horizontally for three.js cube sampling - SKY_XFORM in viewer.js, AQVskyRot for live recalibration), sound swaps, ~~team packs~~ done 2026-08-31 (texture swaps only - sky/lighting are personal; .aq2pack.json via Export pack button, Import preset auto-detects packs and never touches the receiver's sky/lighting).
- **V4 (the hub)**: the app becomes the everyday AQ2 launcher:
  *(note: user starts the game via `q2pro.exe`, not the `aqtion.exe` stub — make the launch exe configurable)*
  - **Player setup**: edit nick, skin/model, and common client settings from a friendly UI (writes cvars like `name`/`skin` to cfg)
  - **Server browser**: query master servers / aq2world list + UDP `status` pings → live list of Action servers with map, players, ping; see who's playing where
  - **Connect from app**: click a server → launches `aqtion.exe +connect ip:port` with your chosen install profile, nick, and texture presets already hooked in

## Still to verify in the live client

1. ~~Hook + apply flow~~ ✔ confirmed live 2026-08-31 (F9 path). Still nice to confirm: auto-apply on a map *change* without pressing anything.
2. Visual quality of generated WALs (palette quantization) in low-res mode (`r_texture_overrides 15`).
3. Engine source: https://github.com/actionquake (branch `aqtion`)

## Notes

- Portable unsigned .exe will trigger Windows SmartScreen once ("More info → Run anyway") — tell friends this is expected.
- User is new to git; set up a GitHub repo for this project when coding starts (like the AQF backup).

## References

- AQ2 Pro addon / softlink usage: https://aq2world.com/pro and https://actionquakenet.wordpress.com/aq2_pro/
- AQtion project source: https://github.com/actionquake
- AQ2World wiki: https://wiki.aq2world.com/wiki/How_to

- **2026-09-04 (night) — Model overhaul, milestone 2: real hands on the M4.** MakeHuman forearms+hands (MPFB, CC0) fitted frame by frame onto the M4's arm animation: `tools/hands/` (extract_arms.py → bake_arms.py → fit_hands.py, README inside), assets in `C:\AI\AIprojectsq2models\hands\`. Result `v_m4_realhands.md3` (5683 verts, 7641 tris, 2 surfaces, 3.4 MB) + 1280x1040 skin (gun on top, baked skin band below) is active on the play install's v_m4 and saved in the collection as "M4 real hands (MakeHuman demo)". Engine facts: MD3 normals are Quake-3 byte order (high byte azimuth, low byte zenith) — `md2_to_md3.py` fixed; Blender UV v must be flipped; view models are lit ~2.5x so hand skins are authored dark (bandgain 0.35). Known rough edges: right hand mostly hidden behind the grip (shift_r -3), procedural skin (no pores/veins), forearms plain. NEXT: same for the other 8 view weapons, better skin texture (photo-based), player model demo (198 frames, MD5 possible on the new engine).
- **2026-09-04 (late) — Real hands v2 after Ralle's review** (gun too small + gap at the screen edge, right hand not gripping, left hand upside down). Causes found: the MD3 frame *local origin* is applied by q2pro as a translation (writer now writes 0,0,0 → gun identical to stock, measured on screenshots); the finger curl went backwards (MakeHuman finger 1 is the thumb, curl axis sign) and the "palm normal" pointed out of the back of the hand → every palm rule was mirrored. Now: palm = knuckle plane with sign from the curl, `--palmdir` rolls the hand about the forearm axis, `--hand` puts the palm centre on the grip (M4 pistol grip is at x 20-25 / z -18..-30, right face y -24 — verified with wireframe x-z plots, not by guessing), left palm slides into contact with the handguard. Result: right fist on the grip with index on the trigger (mostly below the screen edge on the M4, the model sits low/right), left hand cups the handguard with fingers over the far side. Collection entry replaced: "M4 real hands (MakeHuman demo v2)". Workflow note: pose checks are done in Blender (render_md3.py + x-z/x-y wireframe plots with unit grid); the game run only confirms lighting/skin. Ralle's MP5 (v_machn) has anatomically posed blocky hands and is the reference for the other weapons.
- **2026-09-04 (late night) — Imported CC0 arms win.** After six rig iterations of our own MakeHuman hands (rig_pose.py; usable but slow to tune), Ralle chose to try premade models. 'Animated FPS HAND' (OpenGameArt, CC0, Blender 2.79, both arms, IK rig, photo texture, idle/shoot/reload made for an AR rifle) imported with `tools/hands/import_fpshand.py`: their arms + animation resampled onto AQ2's 72 M4 frames, our M4 mesh riding on their weapon bone. First result in game already beats everything before: real reload (gun tilts, left hand to the magazine), fist on the grip, index on the trigger. Assets in `C:\AI\AIprojectsq2models\imported\`. Next: skin brightness, trim the shoulder chunk, fine alignment, then the same importer for the other rifles (their animation is generic AR); pistols/SMGs need other clips.
- **2026-09-04 (end of day) — Skin studio parked.** Ralle: finish TexSwap v1 first, then weapon skins + models return as one update. The whole studio (core, UI, routes, library, model tools) lives on branch `skin-studio-wip` (tag `skin-studio-wip-2026-09-04`); master has none of it. Existing installs get `texswap/skins/` removed and cfgs restated once (`skinsRemoved` flag). Lesson from the first cut: restate cfgs only after the upscaler exists, or upscale links vanish. Next for v1: full-screen texture picker, README, release routine.
- **2026-09-05 — Tester idea: .map support ("Mapper mode").** A tester suggested reading uncompiled `.map` files: view the textures a map-in-progress uses and retexture it BEFORE compiling. Assessment: build it INTO TexSwap as a separate Mapper mode/screen (can be branded "Mapper's Edition" inside the app) — NOT a second product: ~90% reuses existing machinery (VFS/texture catalog, dims/thumbs, picker with favorites, 3D viewer with its fullbright fallback = exactly right for lightmap-less sources, flat generator, install handling), and a fork would double the release routine. Technical sketch: `.map` is text (brush planes + per-face texture name, offset/rot/scale); parse both classic Q2 and Valve220 (TrenchBroom) dialects; 3D preview = clip face polys from brush planes + UVs from the texdef; WRITES are surgical (replace only the texture-name field line by line, keep the rest of the file byte-for-byte, write a .bak) so the mapper's source is never at risk. Killer detail: the app knows both textures' dims and can auto-adjust the face scale on size mismatch (64→128 = scale x2) so alignment survives the swap. V1 scope: open .map -> texture inventory + missing-check -> fullbright 3D preview -> bulk name swap (stock/custom; flats materialize as real files to ship in the map's pkz) -> write retextured .map + report. Ralle's own use case: bulk retexture across the Nostromo a1-a27 WIPs. Unscheduled — order vs the skin-studio comeback is Ralle's call.
- **2026-09-05 (later) — Mapper app vision v2 (Ralle expanded the scope).** With face/brush/entity/light features the idea graduates from "TexSwap reads .map" to a DEDICATED mapper app — but same repo, shared core/, two thin UI shells (two exes or one exe with two branded entries), so one engine room and one release routine. Product identity: "the editor edits — this app SEES what the game sees" (render the .map through the install's real VFS, real hi-res order, real light cvars, wal vs hi-res mode; NO geometry editing ever — that's TrenchBroom's job). Selection model (Ralle's): click face -> grow to brush -> grow to all faces sharing the texture, plus reverse (click texture in list -> highlight its faces); retexture selection with alignment-preserving auto-scale; undo. Tiers: (1) wedge = .map parse (classic + Valve220), engine-truth 3D, selection, bulk retexture with surgical writes + .bak, missing-texture report; (2) mapper QA = entity browser (filter, fly-to, target/targetname link lines, safe key edits) + lints: broken target chains, flag mistakes (warp without trans, missing SURF_LIGHT), stats vs engine limits, and a SOUND-BUDGET check counting target_speaker sounds against the 256 table (born from the 2026-09-04 Kingslanding crash); (3) light + compile = light-entity overlay (radius/color spheres, sun arrow, rough Lambert darkness estimate), then compile orchestration (qbsp/vis/arghrad profiles, error parsing) and open the resulting BSP in the SAME viewer for before/after — the BSP+lightmap renderer already exists; (4) cool stuff = auto-pkz packer (collect the custom textures/sounds a map references into a release pkz), .map version diff (Nostromo a1 vs a27), turntable renders for announcements. Unscheduled; naming open (working title "TexSwap Mapper's Edition").
